import pygame


pygame.init()
screen = pygame.display.set_mode((800, 600))
clock = pygame.time.Clock()

pygame.mouse.set_visible(False)

# Create a blue circle as a cursor
player_circle = pygame.draw.circle(screen, (0,0,255), (400,300), 50)
while True:

    # Make sure pygame doesn't crash
    for event in pygame.event.get():
        if event.type == pygame.QUIT:
            pygame.quit()
            sys.exit()

    # Get the position of the mouse
    mouse_pos = pygame.mouse.get_pos()
    player_circle = pygame.draw.circle(screen, (0,0,255), (mouse_pos), 50)
    
    
    # 2. Update the center of the object to match the cursor
    player_circle.center = mouse_pos

    # 3. Draw everything
    screen.fill((255, 255, 255))  # Clear screen
    player_circle = pygame.draw.circle(screen, (0,0,255), (mouse_pos), 50)

    # Check if the user pressed down their mouse
    if event.type == pygame.MOUSEBUTTONDOWN:
        screen.fill((255, 255, 255))
        player_circle = pygame.draw.circle(screen, (0,0,255), (mouse_pos), 35)

    # Check if they pressed up
    if event.type == pygame.MOUSEBUTTONUP:
        screen.fill((255, 255, 255))
        player_circle = pygame.draw.circle(screen, (0,0,255), (mouse_pos), 50)

    # Drawing rectangles
    pygame.draw.rect(screen, (0,255,0), (1,2, 4, 3))
    

    # Make it stay open
    pygame.display.flip()
